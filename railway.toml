[build]
builder = "NIXPACKS"
buildCommand = "python manage.py collectstatic --noinput"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn ifest.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --threads 4 --timeout 60"
